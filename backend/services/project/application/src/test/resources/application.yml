spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa
  flyway:
    locations: classpath:db/migration/h2
axon:
  axonserver:
    enabled: false